# GoalApp

A simple Android app for tracking progress of your goals. Written in Kotiln.

## Build instructions

Create `keystore.properties` file at project root with the following contents:

```properties
storePassword=XXXXX
keyPassword=XXXXX
keyAlias=XXXXX
storeFile=path/to/file.jks
```
